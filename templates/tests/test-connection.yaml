apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cluster-setup.fullname" . }}-test-connection"
  labels:
    {{- include "cluster-setup.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cluster-setup.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
